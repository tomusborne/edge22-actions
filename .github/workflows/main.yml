name: 'Main action'

on:
  workflow_dispatch:

jobs:

  js:
    uses: tomusborne/edge22-actions/.github/workflows/js-ci.yml

  php:
    uses: tomusborne/edge22-actions/.github/workflows/php-ci.yml

  build:
    uses: tomusborne/edge22-actions/.github/workflows/js-build.yml
    needs: js
    with:
      message: 'Update dist files'
      dir: 'dist/'
